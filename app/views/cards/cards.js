'use strict';

angular.module('app.cards', [])

  .controller('cardCtrl', ['$scope','$stateParams', '$state', 'elencoCarte', function ($scope, $stateParams, $state, elencoCarte) {

    var vm = this; 

      
      
      /* console.log("Stai osservando il prodotto " + $stateParams.productName);

          vm.nomeProdotto = $stateParams.productName;

          vm.goTo = function(url){
              $state.go(url)
          }*/
  }]);
